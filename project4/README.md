
  # User input request

  This is a code bundle for User input request. The original project is available at https://www.figma.com/design/omSF2zKGsrgCb0Wi7qhryY/User-input-request.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  